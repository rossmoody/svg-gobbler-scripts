import { Svg } from './svg';
export declare class GElement extends Svg {
    constructor(originalString: string, id: string, lastModified: string);
    private removeXlinkHref;
    private processG;
}
